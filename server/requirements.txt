# === Web Framework ===
Flask==2.3.2
Flask-Cors==4.0.0

# === Data Handling and Machine Learning ===
pandas==2.0.3
scikit-learn==1.3.0
numpy==1.25.0

# === Web Scraping ===
requests==2.31.0
beautifulsoup4==4.12.2
lxml==4.9.3             # Faster and more flexible HTML/XML parser (optional but recommended)
html5lib==1.1           # Alternative parser for edge-case HTML handling
urllib3==2.0.3          # Used internally by requests, listed here for explicit control
httpx==0.24.1           # Modern alternative to requests (async capable)
fake-useragent==1.4.0   # For rotating User-Agent headers to avoid basic bot blocks
chardet==5.2.0          # Character encoding detection (helps avoid encoding issues)

# === Optional Tools for Parsing / Cleaning ===
python-dateutil==2.8.2  # Robust date parsing
cssselect==1.2.0        # Enables CSS selectors with lxml (optional)
